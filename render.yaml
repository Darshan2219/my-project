services:
  - type: web
    name: portfolio-advisory-system
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    runtime: node
    buildFilter:
      paths:
      - package.json
      - src/**
      - public/**